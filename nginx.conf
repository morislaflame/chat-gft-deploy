events {
    worker_connections 1024;
}

http {
    client_max_body_size 100M;
    client_body_timeout 60s;
    client_header_timeout 60s; 

    upstream backend {
        server backend:5000;
    }

    upstream frontend {
        server frontend:80;
    }

    upstream minio {
        server minio:9000;
    }

    upstream minio_console {
        server minio:9001;
    }

    # HTTP server - БЕЗ редиректа на HTTPS (для получения сертификата)
    server {
        listen 80;
        server_name chatGFT.pro www.chatGFT.pro;

        # Let's Encrypt challenge - ВАЖНО!
        location /.well-known/acme-challenge/ {
            root /var/www/certbot;
            try_files $uri =404;
        }

        # API routes (временно доступны по HTTP)
        location /api/ {
            client_max_body_size 100M;
            proxy_pass http://backend;
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_set_header X-Forwarded-Proto $scheme;
        }

        # Webhook route
        location /webhook {
            proxy_pass http://backend;
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_set_header X-Forwarded-Proto $scheme;
        }

        # MinIO API
        location /minio/ {
            client_max_body_size 100M;
            proxy_pass http://minio/;
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_set_header X-Forwarded-Proto $scheme;
        }

        # MinIO Console
        location /minio-console/ {
            proxy_pass http://minio_console/;
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_set_header X-Forwarded-Proto $scheme;
        }

        # Frontend
        location / {
            proxy_pass http://frontend;
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_set_header X-Forwarded-Proto $scheme;
        }
    }

    # HTTPS server - ЗАКОММЕНТИРОВАН ДО ПОЛУЧЕНИЯ СЕРТИФИКАТА
    # Раскомментируйте после получения SSL сертификата
    # server {
    #     listen 443 ssl;
    #     server_name chatGFT.pro www.chatGFT.pro;
    #
    #     ssl_certificate /etc/letsencrypt/live/chatGFT.pro/fullchain.pem;
    #     ssl_certificate_key /etc/letsencrypt/live/chatGFT.pro/privkey.pem;
    #     
    #     ssl_protocols TLSv1.2 TLSv1.3;
    #     ssl_ciphers ECDHE-RSA-AES256-GCM-SHA512:DHE-RSA-AES256-GCM-SHA512:ECDHE-RSA-AES256-GCM-SHA384:DHE-RSA-AES256-GCM-SHA384;
    #     ssl_prefer_server_ciphers off;
    #     ssl_session_cache shared:SSL:10m;
    #     ssl_session_timeout 10m;
    #
    #     location /api/ { ... }
    #     location /webhook { ... }
    #     location /minio/ { ... }
    #     location /minio-console/ { ... }
    #     location / { ... }
    # }
}